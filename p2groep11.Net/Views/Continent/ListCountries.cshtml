@using p2groep11.Net.Models.Domain
@model IEnumerable<p2groep11.Net.ViewModels.CountryListViewModel>

@{
    ViewBag.Title = "Countries";
    Layout = "~/Views/Shared/_Layout.cshtml";
    int count = 0;
}

<h2>Landen</h2>
<p>
    @using (Html.BeginForm("ListCountries", "Continent", FormMethod.Get))
    {
    <div>
        Land: @Html.TextBox("Search")
        <input type="submit" value="Filter" />
    </div>
    }
</p>


<div>

    @foreach (var item in Model)
    {
           @:<div class="col-md-4">
        

                <div>
                    @Html.ActionLink(@item.Name, "ListLocations",
                        new {Selectedyear = @ViewBag.SchoolYear, continentId = item.ContinentId, countryId = item.CountryId})
                </div>
    
        @:</div>
                }
           
             </div>




